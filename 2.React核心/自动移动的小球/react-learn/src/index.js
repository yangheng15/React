import React from 'react';
import ReactDOM from 'react-dom';
import Ball from './components/Ball';

ReactDOM.render(<Ball left={100} top={100} xSpeed={100} ySpeed={200} />, document.getElementById('root'));
